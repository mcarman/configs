session_name: 3-panes
windows:
  - window_name: dev window
    layout: main-vertical
    shell_command_before:
      - cd ~/
    panes:
      - shell_command:
          - cd projects
          - ls -la
      - shell_command:
          - cd
          - ls -la
      - shell_command:
          - journalctl -f
